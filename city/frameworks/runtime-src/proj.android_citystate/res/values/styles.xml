<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="main_tab_but_linear">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">fill_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:orientation">vertical</item>
        <item name="android:clickable">true</item>
    </style>

    <!-- 界面切换动画 -->
    <style name="AnimActivityTheme">
        <item name="android:windowAnimationStyle">@style/FeelyouWindowAnimTheme</item>
    </style>

    <style name="FeelyouWindowAnimTheme" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenExitAnimation">@anim/in_from_righ</item>
        <item name="android:activityCloseEnterAnimation">@anim/out_to_left</item>
    </style>

    <style name="LoginAnimActivity">
        <item name="@android:windowAnimationStyle">@style/activty_fade_in_fade_out</item>
    </style>

    <style name="activty_fade_in_fade_out" parent="@android:style/Animation.Activity">
        <item name="android:windowEnterAnimation">@anim/in_from_righ</item>
        <item name="android:windowExitAnimation">@anim/out_to_left</item>
    </style>

    <!-- popWindow显示和隐藏动画 -->
    <style name="popWindowAnim">
        <item name="android:windowEnterAnimation">@anim/pop_show</item>
        <!-- 指定显示的动画xml -->

        <item name="android:windowExitAnimation">@anim/pop_hide</item>
        <!-- 指定消失的动画xml -->
    </style>

</resources>